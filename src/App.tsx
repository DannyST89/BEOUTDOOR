import { useState } from "react";  

function App() {
  const [count, setCount] = useState(0);

  return (
    <div>
      <h1>BEOUTDOORS Pagina principal</h1> 
    </div>
  );
}

export default App;
